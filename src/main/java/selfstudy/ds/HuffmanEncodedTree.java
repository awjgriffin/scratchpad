package selfstudy.ds;

/**
 * TODO:
 * supply a binary representation of a code, where each symbol of an alphabet (not necessarily 
 * single letters, has a binary code, e.g. 10011
 * 
 * */
public class HuffmanEncodedTree<T extends Comparable<T>> extends BinarySearchTree<T>{

	protected HuffmanEncodedNode<T> root;
	

	
}
